// Shared Types — BioLogik
// TODO: Add domain types as features are implemented

export type Role = 'customer' | 'nutritionist' | 'kitchen' | 'admin';

export type OrderStatus = 'pending' | 'preparing' | 'completed' | 'delivered';

export type SubscriptionPlan = 'weekly' | 'monthly';

export type SubscriptionStatus = 'active' | 'paused' | 'cancelled';
