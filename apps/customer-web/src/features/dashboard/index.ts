/**
 * Dashboard Feature — BioLogik Customer
 *
 * Main landing page after authentication.
 * All components use mock data; swap for real API when backend endpoints are ready.
 */

export { DashboardPage } from './pages/dashboard-page';
export type {
  DashboardData,
  DashboardStats,
  DashboardMeal,
  DashboardOrder,
  DashboardAppointment,
} from './types';
