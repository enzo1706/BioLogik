/**
 * Profile Feature — BioLogik Customer
 *
 * Personal info, subscription management, nutritionist assignment, and security.
 * Uses mock data; swap for real API when backend endpoints are ready.
 */

export { ProfilePage } from './pages/profile-page';
export type {
  ProfileData,
  PersonalInfo,
  SubscriptionInfo,
  AssignedNutritionist,
} from './types';
